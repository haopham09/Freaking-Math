package com.thohao.freakingmath_kp12;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.util.Log;
import android.widget.Toast;

import java.util.Random;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        /*Random random=new Random();
        for (int i=0;i<=30;i++){
            int number = random.nextInt(max-min+1)+min;

            Log.d("BBB", number + " ");
        }*/

        //cach xem tham so trong method ctrl+P
        //ep kieu sang chuỗi string
        //Toast.makeText(MainActivity.this, String.valueOf(number), Toast.LENGTH_SHORT).show();
        //Toast.makeText(MainActivity.this, number+"", Toast.LENGTH_SHORT).show();

        //git branch ; tạo nhánh
        //git reset ; xoa commit
        //git --amend; gộp commit
        //git merge ; gọp code từ nhánh khác





    }
}
